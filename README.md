#### _Note: This boilerplate might uses my fork of Imba which isn't always synced with upstream._

---

```sh
npm i         	# install dependencies
npm run serve 	# start dev-server
npm run build 	# build website
```